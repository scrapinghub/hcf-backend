# Automatically created by: shub deploy

from setuptools import setup, find_packages

setup(
    name         = 'hcf_backend',
    version      = '0.2',
    packages     = find_packages(),
    install_requires=('frontera==0.7.1', 'scrapinghub>=2.0.0')
)
